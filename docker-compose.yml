name: agent-workspace

services:
  agent:
    build: .
    stdin_open: true
    tty: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    volumes:
      - agent-home:/home/agent
      - ./workspace:/workspace
    networks:
      - agent-network

volumes:
  agent-home:

networks:
  agent-network:
    driver: bridge
